// Archivo de configuración de Vite.
import vue from '@vitejs/plugin-vue'; // Importamos el plugin.

export default {
    // Definimos los plugins que Vite debe usar. Noten que cada plugin es una función que ejecutamos.
    plugins: [vue()],
}